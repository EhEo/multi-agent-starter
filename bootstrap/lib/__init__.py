"""bootstrap.lib — multi-agent-starter bootstrap internals.

이 패키지는 마커 역할만 하며, 직접 임포트할 심볼을 노출하지 않는다.
각 모듈(platform_info, packages, cli_tools, pathing, repo, verify)은
bootstrap/install.py 오케스트레이터가 직접 임포트한다.
"""
